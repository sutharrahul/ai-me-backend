# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Environment variables
.env

# Data (document metadata index)
data/documents.json

# Personal bio source data - already ingested into the vector DB, so the
# live app doesn't need it in the repo; kept out of git so the raw content
# isn't publicly visible on GitHub. Still present locally for re-ingestion.
data/seed/about_rahul.md
data/Knowledge Base_ Rahul Suthar (Full Stack & AI Engineer).pdf

# Runtime logs
logs/

# OS
.DS_Store
